window-padding-y = 0,24

# tmux compatibility:
# Do NOT set `term` here — Ghostty auto-detects xterm-ghostty for direct sessions.
# For tmux, add this to ~/.tmux.conf instead:
#   set -g default-terminal 'xterm-256color'
